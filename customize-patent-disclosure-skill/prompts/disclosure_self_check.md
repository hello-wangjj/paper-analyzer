# 交底书生成后自检（Step 8，内部执行）

自检结果用于**修订正文**，默认**不单独输出自检报告**；用户索要时可单独提供。**不得**将自检清单作为交底书一章写入正文。

章节号以下方为准：**3.1/3.2/3.3**（背景技术）、**4.1/4.2(.1–.5)/4.3/4.4/4.5**（创新点）、**五、实施方式**、**六、附图**。

## 8.1 逻辑与闭环

- [ ] 技术方案是否形成完整闭环（**3.2 缺陷 → 4.1 问题 → 4.2 方案 → 4.4 效果 → 4.5 论证**）？
- [ ] **3.3 业内尝试**是否回答了「同行对该缺陷的改进路线 + 为何仍不足」？与 3.1 互补、不重复堆砌？
- [ ] **4.3 代替方案**是否列出可替换技术手段（不同构思）以扩大保护范围？无替代时是否如实说明而非编造？
- [ ] **4.5 逻辑论证**是否从技术角度论证「为何能克服缺陷、各要素如何协同」？
- [ ] 模块之间关联关系（4.2.2）是否清晰？上下游是否衔接？
- [ ] 分支样本（如边界样本、低置信度样本等）是否有明确处理路径？
- [ ] 依赖项（如类别中心、动态阈值等）是否有来源说明？
- [ ] 第四章「创新点（4.2 方案 / 4.3 代替方案 / 4.5 论证）」与第五章「实施方式」是否呼应？第五章实施例是否对应所保护的技术方案。
- [ ] **第二章场景铺垫**：是否已按场景与参与者、标题对象如何进入系统、高频领域词定义铺成一小段（3–5 句）？
- [ ] **标题贯穿**：案件名称中的领域实词是否在 **4.2.1 框图、4.2.3 流程、第五章实施方式**作为同一执行主体出现，且未被更空的上位词替换？
- [ ] **术语贴合话题**：4.2.2 / 4.2.5 / 第五章用词，只看标题和第二章场景是否像本领域？若像另一技术领域，是否已换成领域表述而非靠定义保留？机制是否仍说得清？
- [ ] **4.2.2 与 4.2.3 对应**：4.2.2 是否按 4.2.1 框图拆成独立模块（一项一个模块）？4.2.3 正文是否逐项对应图中 S1—Sn（无漏步、无多步混成一段且不点序号）？
- [ ] **实施例**：是否有 2～3 个与第二章 / 框图同名的实例（职责、能力、交付物、依赖），并覆盖主路径 + 一条第四章已有分支（而非 4.2.3 缩写）？

## 8.2 公式与参数一致性

**Step 8 必做（含公式时）**：除符号体例与跨节一致外，须**主动复核公式是否正确、公式逻辑是否与第四章叙述一致**（等同用户会提出的「检查公式和公式逻辑是否有误，有误请调整」）；发现问题**直接改稿**，勿仅提示用户自行修改。

### 符号与体例

- [ ] **formula_plan（含公式时）**：案件目录已有 `formula_plan.yaml`；`paradigm_ids`∈`references/formulas/paradigms.yaml`（及覆盖合并结果）；`check_formula_plan.py` 通过或等价手检通过
- [ ] **符号表（4.2.4）**：全文含公式时是否已设 **4.2.4** 并 **先定义符号**（含义、下标、量纲）；式 (1) 及后文每个符号是否均已在表中定义？
- [ ] **维度下标**：是否存在 `^{cpu}`、`^{mem}`、`^{io}` 等 **上标表示维度** 的写法？若有须改为 `b_{i,\mathrm{cpu}}`、`a_{j,\mathrm{mem}}` 等 **下标 + `\mathrm{}`** 形式（见 **`disclosure_builder.md` §7.7**）
- [ ] **装饰音**：是否出现 `\tilde`/`\hat`/`\bar` 等？若有改为独立符号（如平滑量 \(A\)）或范式 `ema_smooth`
- [ ] **字母多义**：同一字母是否兼指任务侧与节点侧等不同对象？若有须拆分符号（如任务用 \(b\)、节点用 \(g\)）
- [ ] **公式表述一致**：同一物理量在不同公式、段落中是否 **同形同义**（如权重公式、调整系数、匹配分 \(M_{ij}\) 等）
- [ ] **LaTeX 体例**：行内/块级分隔符是否全文统一（`\(...\)`/`\[...\]` 或 `$`/`$$` 二选一）；块级公式是否尽量单行；是否避免 `\text{且}` 等易渲染失败写法
- [ ] **4.2.5 与符号表**：4.2.5 参数表「符号」列是否与 4.2.4 **逐字同形**
- [ ] **阈值范围**：与正文一致（如 0.5–1.5、0.8–1.2 等）
- [ ] **参数命名**：全文统一，避免同义不同名混用
- [ ] **实施例数值**：与 4.2.5 关键技术参数对应，不冲突

### 公式正确性与逻辑（必核）

- [ ] **式面正确性**：各式是否无明显笔误（运算符、括号配对、上下标错位、同一式内符号与符号表不符、缺项/多写项、指数或分母写错等）？
- [ ] **可算性**：先跑 `python tools/check_formula_plan.py -i …/formula_plan.yaml --eval`；代算失败须改式或改数值例。脚本跳过的复杂式再用 `numeric_example`（或第五章同一组数）手算；算不出则改式或补定义（如 \(\sigma\)）
- [ ] **量纲常理**：相加各项是否已无量纲/归一？防零除 \(\varepsilon\) 是否与分母同量纲？有无裸 `max(1, 容量)`？
- [ ] **约束与不等式方向**：公式中的 `\leq`/`\geq`/正负号是否与文字含义一致（如「饱和度」「有余量」「限频间隔」等）；多条件「且/或」是否与 4.2.3 流程分支一致？
- [ ] **公式与流程互推**：4.2.4 主公式（含式 (1)）及衍生式，能否与 **4.2.3 流程说明**（各步骤 S1—Sn）、**4.2.2 模块职责** 逐步对应？叙述中的打分、重排、限频、窗口聚合等是否在公式中有体现，**无**「文字一套、公式另一套」？
- [ ] **边界与特殊情形**：空队列、单节点、分母为零、指标缺失、达到上下界等情形，公式或正文是否给出合理处理或说明（至少不与主公式矛盾）？
- [ ] **量纲与取值**：符号表声明的量纲/取值范围是否与公式用法一致（如权重为正、比例在 0—1、计数为非负整数等）？
- [ ] **修订联动**：若修正公式或逻辑，是否已同步改 **`formula_plan.yaml`**、**4.2.3 文字**、**4.2.5 参数**、**第五章实施例** 及 **4.2.4 符号表**（见 §7.7 跨节一致）？

## 8.3 格式、结构与引用（对齐 docx 模板）

- [ ] **封面表**：正文起始是否含完整「案件管理信息表」（拟申报专利名称、申报类型、委托编号、发明人、联系人、申请人、申请国家、日期等）？身份证号/组织机构代码证/委托编号等是否为 **`[待填写]`**（Agent 不得编造）？封面「拟申报专利名称」与第一章名称是否**逐字一致**？
- [ ] **章节齐全**：一名称 / 二所属技术领域 / 三背景(3.1+3.2+3.3) / 四创新点(4.1+4.2+4.3+4.4+4.5) / 五实施方式 / 六附图 —— docx 模板要求的章是否**全部存在**，无遗漏（尤其旧版易缺的：**二、所属技术领域**、**3.3 业内尝试**、**4.3 代替方案**、**4.5 逻辑论证**、**六、附图**）？
- [ ] **名称字数**：第一章名称是否 **≤25 字** 且为「XX装置/系统/方法」等规范形式？
- [ ] **字数下限**：3.2（≥100）、3.3（≥100）、4.2（≥200）、4.5（≥100）是否满足 docx 模板字数要求？
- [ ] **附图章**：第六章是否**汇总**所有附图（图号 + 图名 + 来源章节）并做简要说明？同一对象在不同图中的标号是否一致？附图标号是否在第四章技术方案中有解释？
- [ ] **迭代路径**：若本次走 `merger.md` / `correction_handler.md`，对话中是否已含 **`## 合并摘要（留档）`** 或 **`## 纠正摘要（留档）`**（按该 prompt 字数要求）
- [ ] **修订对话记录**：案件目录是否已追加 **`交底书修订对话记录.md`** 一条（含记录时间、用户说明摘要、本轮交付文件名、摘要摘录），见 **`iteration_context.md`**
- [ ] **交付文件名**：凡落盘交付的交底书是否均为 **`{案件名}_{YYYYMMDDHHmmss}.md`** 及同名 `.docx`**（§7.3 第 5 点，**含首次定稿与迭代**），未无故覆盖旧稿
- [ ] **版本目录**：交付是否已按 **`versioned_output.md`** 写入 `vN-简短说明-时间戳/` 版本文件夹（含 `mermaid_figures/`），根目录无新增平铺定稿文件，**`版本索引.md`** 已创建/更新
- [ ] **系统框图（4.2.1）与流程图（4.2.3）**：均为 fenced mermaid，定稿已用 `mermaid_render.py` 转 PNG，**无** ASCII 文字流程图/框图；流程图节点若用 S1/S2 作 id，**可见标签须含该序号**（`S1["S1 …"]`，禁止 `S1[只写中文]`）；节点用**标题领域词**，未被换成空上位词；`.md` 已交付，`.docx` 已生成或已按提示手动 `md_to_docx.py` 补全
- [ ] **附图 PNG 读图核对（多模态）**：已渲染的 mermaid PNG 已**委派 `multimodal-vision` 子智能体**（或等价读图）逐图核对——步骤号 S1—Sn 出现在**可见标签**内、节点用**标题领域词**（未被空上位词替换）、中文无乱码/缺字、连线与 4.2.2/4.2.3 叙述一致；发现问题的图已改围栏源码并重渲染（子智能体报告须与源码文本交叉核对，勿只信转述；无该子智能体时主代理直接读图）
- [ ] **专利簇清单**：本轮交付 2 件及以上相关专利时，是否已按 `patent_family_explainer.md` 生成通俗说明清单并写入当前版本目录
- [ ] 章节引用：如「详见 4.2.4」须指向真实存在的章节
- [ ] **3.1 现有技术**：每个方向下列举的专利/文献是否均附有**可访问且与著录项一致**的公开 URL（非编造）；是否与 `prior_art_search.md` 要求及 3.1「检索说明」自洽；**检索说明**是否**未**出现 `cnipa_epub_search.py`、WebSearch/降级等内部流程用语；国知局命中项是否**照抄 JSON `link`**（未被换域名为 Google Patents）
- [ ] **查新类型**：查新命令 `--type` 与封面表「申报类型」一致（发明 `invention` / 实用新型 `utility_model`）
- [ ] **脚本判读**：未因 PowerShell stderr / 乱码 / `NativeCommandError` 把退出码 0 的查新或定稿当成失败；未因 stderr 噪声不必要地降级 WebSearch 或重跑安装
- [ ] **3.3 业内尝试出处**：3.3 列出的同行改进措施是否均附文件出处与公开 URL（非编造）
- [ ] **查新摘要（国知局）**：凡 **`EPUB_HITS_JSON`** 中带非空 **`abstract`** 的条目，3.1/3.3 对该条的方案概括与局限**是否体现对摘要的理解**（非仅标题、且不与摘要矛盾）；无 `abstract` 的条目是否在查新路径上已按 `prior_art_search.md` 用其它可核验来源补全理解
- [ ] **模糊化标注**：用户明示需保护的业务敏感项是否**就地标注「【建议模糊化】」**（而非擅自删改）；未明示的默认保留真实信息；**入文图片**（截图/照片等）中可能含公司名、产品名、内部代号或敏感数值时，已委派 multimodal-vision 识别并按 §7.5 表处理
- [ ] **文末清洁**：正文**无**技能仓库名、`examples/`、`disclosure_draft` 路径、「教学示例」「虚构」「不构成法律承诺」等**元信息脚注**（若存在则删除）
- [ ] **权利要求偏向点（对话）**：凡**定稿交付**的对话回复，是否已按 **`disclosure_builder.md` §7.6** 补充「可选下一步」类建议交互（**仅对话**，不入正文）；其中对举的侧重点是否**源于本稿与已定观点**（优先基于 4.3 代替方案已列出的可替换手段），**无**凭空捏造

## 处理原则

发现问题则**直接修订正文**后再交付。  
**公式类**：Step 8 须完成 §8.2「公式正确性与逻辑」核对；有误则改公式并联动修订相关章节，**不要**只向用户报告问题而不改稿（除非缺少必要技术事实、无法从正文与材料推断正确写法）。  
**术语 / 标题贯穿 / 实施例**：§8.1 相关项不通过则改第二章、4.2.1 框图、4.2.3 流程、第五章或 mermaid 标签后再交付；换叫法须**整族替换**（3.1 至第六章及 mermaid 标签一并），机制不丢。
